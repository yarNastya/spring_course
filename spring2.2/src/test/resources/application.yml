spring:
  application:
    name: library
  datasource:
    url: jdbc:h2:mem:testdb1

  h2:
    console:
      path: /h2-console
      settings:
        web-allow-others: true
  liquibase:
    enabled: true
    change-log: db\changelog\db.changelog-test.yaml

